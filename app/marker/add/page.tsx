import FiltersPage from "@/app/page";

/*
  When user refreshes page, this acts as a sort of fallback so this page displays
  rather than just the map displaying.
*/

export default function Page() {
  return <FiltersPage />;
}